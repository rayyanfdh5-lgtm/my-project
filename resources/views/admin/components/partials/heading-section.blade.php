<div class="flex flex-col">
    <h1 class="title-form {{ $titleColor ?? 'text-gray-800' }}">{{ $title }}</h1>
    <h1 class="paragraph-form {{ $subtitleColor ?? 'text-gray-500' }}">{{ $subtitle }}</h1>
</div>
